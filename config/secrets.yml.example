# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

default: &default
  notification_receivers: 'admin@example.com'
  exception_email_sender_address: '<kf@jianggaowang.com>'

development:
  secret_key_base: secret_key_base_token_string
  qiniu:
    access_key: 'access_key'
    secret_key: 'secret_key'
    bucket: 'qiniu_bucket'
    bucket_domain: 'qiniu_bucket_domain'
  mail:
    user_name: 'jianggaowang@example.com'
    password: 'examplepassword'
  disqus:
    site: 'disqus_site'

test:
  secret_key_base: secret_key_base_token_string

staging:
  <<: *default
  secret_key_base: secret_key_base_token_string
  qiniu:
    access_key: 'access_key'
    secret_key: 'secret_key'
    bucket: 'qiniu_bucket'
    bucket_domain: 'qiniu_bucket_domain'
  mail:
    user_name: 'jianggaowang@example.com'
    password: 'examplepassword'
  disqus:
    site: 'disqus_site'
  exception_email_prefix: '[Jianggaowang Staging Exception]'

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  <<: *default
  secret_key_base: secret_key_base_token_string
  qiniu:
    access_key: 'access_key'
    secret_key: 'secret_key'
    bucket: jianggao-production
    bucket_domain: 'qiniu_bucket_domain'
  mail:
    user_name: 'jianggaowang@example.com'
    password: 'examplepassword'
  disqus:
    site: 'disqus_site'
  exception_email_prefix: '[Jianggaowang Exception]'
